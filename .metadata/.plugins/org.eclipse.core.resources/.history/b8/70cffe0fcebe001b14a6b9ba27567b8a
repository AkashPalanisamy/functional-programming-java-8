package in28minutes.lambda;

import java.util.Arrays;
import java.util.List;
import java.util.function.Function;

public class BehaviourParameterization {

	public static void main(String[] args) {
		
		List<Integer> integerArray = Arrays.asList(4, 6, 3, 12, 7, 8, 21, 4);
		
		behaviourStream(integerArray, n -> n*n);

	}

	private static void behaviourStream(List<Integer> integerArray,
			Function<Integer,Integer> function) {
		
		integerArray.stream()
					.map(function)
					.forEach(System.out::println);
		
		
	}

}
